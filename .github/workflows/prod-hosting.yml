# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Production Firebase Hosting on merge
"on":
    push:
        branches:
            - prodd

env:
    API_KEY: ${{ secrets.API_KEY }}
    AUTH_DOMAIN: ${{ secrets.AUTH_DOMAIN }}
    DATABASE_URL: ${{ secrets.DATABASE_URL }}
    PROJECT_ID: ${{ secrets.PROJECT_ID}}
    STORAGE_BUCKET: ${{ secrets.STORAGE_BUCKET }}
    MESSAGING_SENDER_ID: ${{ secrets.MESSAGING_SENDER_ID }}
    APP_ID: ${{ secrets.APP_ID }}
    MEASUREMENT_ID: ${{ secrets.MEASUREMENT_ID }}
    SENTRY_ENVIRONMENT: "prod"

jobs:
    build_and_deploy:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: npm install
            - run: npm run build
            - uses: FirebaseExtended/action-hosting-deploy@v0
              with:
                  repoToken: '${{ secrets.GITHUB_TOKEN }}'
                  firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
                  channelId: live
                  projectId: '${{ secrets.PROJECT_ID }}'
            - name: Install npm packages for cloud functions
              run: |
                  cd functions
                  npm install
            - name: Set the Firebase Config
              uses: w9jds/firebase-action@master
              with:
                  args: functions:config:set app.id="dapp_73673778c0014b1aa176adf8d3c2e8cb" app.secret="e5bfee7766ef932cfecff8647f57e51bd2b4e0ffca0bda1f6c27d2261ac8a9de" app.url="https://sable.letsdive.io"
              env:
                  GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
                  TARGET: dev
            - name: Deploy to Firebase Cloud functions
              uses: w9jds/firebase-action@master
              with:
                  args: deploy --only functions
              env:
                  GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
                  TARGET: prod
